name: flutter_podium
description: "Enhance your Flutter app with a sleek 3D Podium Widget for stunning ranking displays, adding visual appeal and engagement to your interface."
version: 1.0.3
homepage: https://github.com/ThrivikramGR/flutter_podium

environment:
  sdk: '>=2.18.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0